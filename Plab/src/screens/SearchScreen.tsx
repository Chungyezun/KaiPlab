import React from 'react';
import { View, Text } from 'react-native';

const SearchScreen: React.FC = () => {
  return (
    <View className='flex-1 justify-center items-center'>
      <Text className='text-2xl'>검색 화면</Text>
    </View>
  );
};

export default SearchScreen;